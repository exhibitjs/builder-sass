{
  "name": "exhibit-sass",
  "version": "0.0.4",
  "author": "Callum Locke",
  "dependencies": {
    "in-place": "^1.0.0",
    "node-sass": "sass/node-sass"
  },
  "devDependencies": {
    "babel": "^5.2.11",
    "babel-eslint": "^3.0.1",
    "eslint": "^0.20.0",
    "nodemon": "^1.3.7"
  },
  "files": [
    "lib"
  ],
  "keywords": [
    "exhibit-plugin"
  ],
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src --source-maps --out-dir lib",
    "prepublish": "eslint src && npm run build",
    "watch": "nodemon --exec 'npm run build' --watch src --watch index.js --watch package.json"
  }
}
